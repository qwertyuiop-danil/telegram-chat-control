[project]
name = "telegram-chat-control"
version = "2.0.0"
description = "Cross-platform local Telegram index for Codex"
requires-python = ">=3.10"
dependencies = [
  "filelock>=3.16,<4",
  "keyring>=25,<26",
  "qrcode[pil]>=8,<9",
  "telethon>=1.42,<2",
]

[tool.uv]
package = false
